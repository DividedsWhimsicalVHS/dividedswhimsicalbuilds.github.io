<!doctype html>
<html lang="en">
 <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Divided's Whimsical Builds</title>
  <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@700&amp;display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <script src="/_sdk/element_sdk.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      height: 100%;
      width: 100%;
    }

    body {
      font-family: 'Fredoka', sans-serif;
      background: 
        radial-gradient(circle at 20% 80%, #ff1493 0%, transparent 25%),
        radial-gradient(circle at 80% 20%, #4169e1 0%, transparent 25%),
        radial-gradient(circle at 40% 40%, #9400d3 0%, transparent 25%),
        radial-gradient(circle at 85% 75%, #ff6347 0%, transparent 25%),
        radial-gradient(circle at 15% 20%, #00ced1 0%, transparent 25%),
        linear-gradient(135deg, #ffd700 0%, #ffb6c1 50%, #87ceeb 100%);
      background-attachment: fixed;
      overflow-x: hidden;
    }

    .main-wrapper {
      width: 100%;
      height: 100%;
      overflow-auto;
      padding: 40px 20px;
    }

    .content-container {
      max-width: 1000px;
      margin: 0 auto;
    }

    .main-title {
      font-size: 3.5rem;
      font-weight: 900;
      background: linear-gradient(135deg, #ff1493 0%, #4169e1 50%, #9400d3 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-align: center;
      margin-bottom: 50px;
      text-shadow: 0 0 30px rgba(255, 20, 147, 0.3);
      letter-spacing: 2px;
    }

    .builds-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-bottom: 40px;
    }

    .build-card {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 15px;
      padding: 20px;
      text-align: center;
      font-size: 1.1rem;
      font-weight: 600;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
      border: 3px solid;
    }

    .build-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2);
    }

    .gunshops { border-color: #ff1493; color: #ff1493; }
    .gunshops:hover { background: linear-gradient(135deg, rgba(255, 20, 147, 0.1), rgba(255, 20, 147, 0.2)); }

    .car-dealerships { border-color: #4169e1; color: #4169e1; }
    .car-dealerships:hover { background: linear-gradient(135deg, rgba(65, 105, 225, 0.1), rgba(65, 105, 225, 0.2)); }

    .vhs-shops { border-color: #9400d3; color: #9400d3; }
    .vhs-shops:hover { background: linear-gradient(135deg, rgba(148, 0, 211, 0.1), rgba(148, 0, 211, 0.2)); }

    .event-shops { border-color: #ff6347; color: #ff6347; }
    .event-shops:hover { background: linear-gradient(135deg, rgba(255, 99, 71, 0.1), rgba(255, 99, 71, 0.2)); }

    .cafes { border-color: #00ced1; color: #00ced1; }
    .cafes:hover { background: linear-gradient(135deg, rgba(0, 206, 209, 0.1), rgba(0, 206, 209, 0.2)); }

    .plot-fees {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 15px;
      padding: 25px;
      margin-bottom: 40px;
      text-align: center;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    }

    .fee-item {
      display: inline-block;
      margin: 10px 15px;
      font-size: 1rem;
      font-weight: 600;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      padding: 10px 20px;
      border-radius: 25px;
      transition: transform 0.3s ease;
    }

    .fee-item:hover {
      transform: scale(1.05);
    }

    .examples-subtitle {
      font-size: 2.5rem;
      text-align: center;
      color: white;
      margin: 50px 0 40px 0;
      font-weight: 800;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
    }

    .sections-container {
      display: flex;
      flex-direction: column;
      gap: 40px;
      margin-top: 40px;
    }

    .section {
      background: rgba(255, 255, 255, 0.97);
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
      border-left: 6px solid;
    }

    .section h2 {
      font-size: 2rem;
      margin-bottom: 25px;
      font-weight: 800;
    }

    .section.gunshops { border-left-color: #ff1493; }
    .section.gunshops h2 { color: #ff1493; }

    .section.car-dealerships { border-left-color: #4169e1; }
    .section.car-dealerships h2 { color: #4169e1; }

    .section.vhs-shops { border-left-color: #9400d3; }
    .section.vhs-shops h2 { color: #9400d3; }

    .section.event-shops { border-left-color: #ff6347; }
    .section.event-shops h2 { color: #ff6347; }

    .section.cafes { border-left-color: #00ced1; }
    .section.cafes h2 { color: #00ced1; }

    .image-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 20px;
    }

    .image-placeholder {
      aspect-ratio: 1;
      border: 3px dashed #ccc;
      border-radius: 15px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #f9f9f9;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .image-placeholder:hover {
      background: #f0f0f0;
      border-color: #999;
    }

    .image-placeholder img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .image-placeholder input {
      display: none;
    }

    .placeholder-text {
      color: #999;
      font-size: 0.9rem;
      text-align: center;
      pointer-events: none;
    }

    .add-button {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      border: none;
      padding: 12px 25px;
      border-radius: 25px;
      cursor: pointer;
      font-weight: 600;
      transition: transform 0.3s ease;
      font-size: 1rem;
    }

    .add-button:hover {
      transform: scale(1.05);
    }

    @media (max-width: 768px) {
      .builds-grid {
        grid-template-columns: 1fr;
      }

      .main-title {
        font-size: 2rem;
        margin-bottom: 30px;
      }

      .examples-subtitle {
        font-size: 1.8rem;
      }

      .section {
        padding: 25px;
      }

      .section h2 {
        font-size: 1.5rem;
      }
    }
  </style>
  <style>body { box-sizing: border-box; }</style>
  <script src="/_sdk/data_sdk.js" type="text/javascript"></script>
 </head>
 <body>
  <div class="main-wrapper">
   <div class="content-container">
    <!-- Main Title -->
    <h1 class="main-title" id="mainTitle">Divided's Whimsical Builds</h1><!-- Build Options -->
    <div class="builds-grid">
     <div class="build-card gunshops">
      Gunshops - 150k
     </div>
     <div class="build-card car-dealerships">
      Car Dealerships - 200k
     </div>
     <div class="build-card vhs-shops">
      VHS shops - 150k
     </div>
     <div class="build-card event-shops">
      Event shops - 300k
     </div>
     <div class="build-card cafes">
      Cafes - 350k
     </div>
    </div><!-- Plot Fees -->
    <div class="plot-fees">
     <div class="fee-item">
      Small plot fee - 50k
     </div>
     <div class="fee-item">
      Medium plot fee - 75k
     </div>
     <div class="fee-item">
      Large plot fee - 100k
     </div>
    </div><!-- Examples Subtitle -->
    <h2 class="examples-subtitle" id="examplesSubtitle">Examples of builds</h2><!-- Build Sections -->
    <div class="sections-container">
     <!-- Gunshops Section -->
     <section class="section gunshops">
      <h2>Gunshops</h2>
      <div class="image-grid" id="gunshopsGallery"></div><button class="add-button" onclick="addImageInput('gunshopsGallery')">+ Add Image</button>
     </section><!-- Car Dealerships Section -->
     <section class="section car-dealerships">
      <h2>Car Dealerships</h2>
      <div class="image-grid" id="carDealershipsGallery"></div><button class="add-button" onclick="addImageInput('carDealershipsGallery')">+ Add Image</button>
     </section><!-- VHS Shops Section -->
     <section class="section vhs-shops">
      <h2>VHS shops</h2>
      <div class="image-grid" id="vhsShopsGallery"></div><button class="add-button" onclick="addImageInput('vhsShopsGallery')">+ Add Image</button>
     </section><!-- Event Shops Section -->
     <section class="section event-shops">
      <h2>Event shops</h2>
      <div class="image-grid" id="eventShopsGallery"></div><button class="add-button" onclick="addImageInput('eventShopsGallery')">+ Add Image</button>
     </section><!-- Cafes Section -->
     <section class="section cafes">
      <h2>Cafes</h2>
      <div class="image-grid" id="cafesGallery"></div><button class="add-button" onclick="addImageInput('cafesGallery')">+ Add Image</button>
     </section>
    </div>
   </div>
  </div>
  <script>
    // Initialize Element SDK
    const defaultConfig = {
      main_title: "Divided's Whimsical Builds",
      subtitle_text: "Examples of builds"
    };

    let config = { ...defaultConfig };

    window.elementSdk.init({
      defaultConfig,
      onConfigChange: async (newConfig) => {
        config = newConfig;
        document.getElementById('mainTitle').textContent = config.main_title || defaultConfig.main_title;
        document.getElementById('examplesSubtitle').textContent = config.subtitle_text || defaultConfig.subtitle_text;
      },
      mapToCapabilities: (cfg) => ({
        recolorables: [],
        borderables: [],
        fontEditable: undefined,
        fontSizeable: undefined
      }),
      mapToEditPanelValues: (cfg) => new Map([
        ['main_title', cfg.main_title || defaultConfig.main_title],
        ['subtitle_text', cfg.subtitle_text || defaultConfig.subtitle_text]
      ])
    });

    // Image handling
    function addImageInput(galleryId) {
      const gallery = document.getElementById(galleryId);
      const placeholder = document.createElement('div');
      placeholder.className = 'image-placeholder';
      placeholder.id = 'placeholder-' + Date.now();

      const input = document.createElement('input');
      input.type = 'file';
      input.accept = 'image/*';

      const img = document.createElement('img');
      img.style.display = 'none';

      input.addEventListener('change', (e) => {
        const file = e.target.files[0];
        if (file) {
          const reader = new FileReader();
          reader.onload = (event) => {
            img.src = event.target.result;
            img.style.display = 'block';
            placeholder.innerHTML = '';
            placeholder.appendChild(img);
            addRemoveButton(placeholder);
          };
          reader.readAsDataURL(file);
        }
      });

      placeholder.appendChild(input);
      placeholder.appendChild(document.createTextNode('Click to add image'));

      placeholder.addEventListener('click', () => input.click());

      gallery.appendChild(placeholder);
    }

    function addRemoveButton(placeholder) {
      const removeBtn = document.createElement('button');
      removeBtn.innerHTML = '✕';
      removeBtn.style.cssText = `
        position: absolute;
        top: 8px;
        right: 8px;
        background: rgba(255, 0, 0, 0.8);
        color: white;
        border: none;
        width: 30px;
        height: 30px;
        border-radius: 50%;
        cursor: pointer;
        font-size: 1.2rem;
        display: flex;
        align-items: center;
        justify-content: center;
      `;
      removeBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        placeholder.remove();
      });
      placeholder.appendChild(removeBtn);
    }

    lucide.createIcons();
  </script>
 <script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'9f347117d4060537',t:'MTc3NzM2MjI2Ny4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>